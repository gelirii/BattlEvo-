'use strict';

const fs=require('fs'),vm=require('vm'),assert=require('assert');let seed=0x5EEDBEEF;Math.random=()=>{seed=(1664525*seed+1013904223)>>>0;return seed/4294967296;};
const fakeCtx=new Proxy({}, {get(t,p){if(!(p in t))t[p]=()=>{};return t[p];},set(t,p,v){t[p]=v;return true;}}),elements={game:{width:960,height:600,getContext:()=>fakeCtx},'brain-red':{value:'4'},'brain-green':{value:'10'},'brain-blue':{value:'20'}};
global.document={getElementById:id=>elements[id]||{value:'',disabled:false,textContent:''}};global.performance={now:()=>0};global.setRunningUI=()=>{};global.updateHud=()=>{};vm.runInThisContext(fs.readFileSync('core.js','utf8'),{filename:'core.js'});const ev=code=>vm.runInThisContext(code);
const sizes=[4,10,20,64],inputStd=1/Math.sqrt(ev('INPUTS'));for(const h of sizes){const l=ev(`genomeLayout(${h})`);assert.ok(Math.abs(ev(`geneStd(${h},0)`)-inputStd)<1e-12);assert.ok(Math.abs(ev(`geneStd(${h},${l.outputWeightBase})`)*Math.sqrt(h)-1)<1e-12);}
const INPUTS_N=ev('INPUTS'),OUTPUTS_N=ev('OUTPUTS'),probes=Array.from({length:32},()=>Float32Array.from({length:INPUTS_N},()=>Math.random()*2-1)),stats={};
for(const h of sizes){let sumSq=0,count=0,maxAbs=0;for(let b=0;b<80;b++){const brain=ev(`new Brain(${h})`);for(const input of probes){const out=brain.run(input);for(let i=0;i<OUTPUTS_N;i++){const v=out[i];sumSq+=v*v;count++;maxAbs=Math.max(maxAbs,Math.abs(v));}}}stats[h]={rms:Math.sqrt(sumSq/count),maxAbs};}
const rms=sizes.map(h=>stats[h].rms),spread=Math.max(...rms)/Math.min(...rms);assert.ok(spread<1.22,`random output RMS depends too strongly on brain size (${spread.toFixed(3)}x)`);
const fireRates={};for(const h of sizes){let fires=0,total=0;for(let b=0;b<100;b++){const brain=ev(`new Brain(${h})`);for(const input of probes){if(brain.run(input)[17]>.15)fires++;total++;}}fireRates[h]=fires/total;assert.ok(fireRates[h]>.22&&fireRates[h]<.45,`${h}N initial real-threshold fire rate ${fireRates[h].toFixed(3)} is unreasonable`);}
const rateSpread=Math.max(...Object.values(fireRates))-Math.min(...Object.values(fireRates));assert.ok(rateSpread<.07,`real firing propensity differs too much by brain width (${rateSpread.toFixed(3)})`);
console.log('BattlEvo neural fairness audit passed.');console.log(JSON.stringify({outputRms:Object.fromEntries(sizes.map(h=>[`${h}N`,+stats[h].rms.toFixed(4)])),rmsSpread:+spread.toFixed(3),fireRateAtActualThreshold:Object.fromEntries(sizes.map(h=>[`${h}N`,+fireRates[h].toFixed(3)]),),fireRateSpread:+rateSpread.toFixed(3)},null,2));
